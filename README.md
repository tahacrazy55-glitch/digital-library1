# 📚 مكتبة رقمية — Digital Library (Front‑End)

واجهة ويب متعددة الصفحات لإدارة مكتبة كتب رقمية تعمل بالكامل داخل المتصفح مع تخزين محلي (LocalStorage).

## الفكرة العامة
المشروع عبارة عن موقع Front‑End يحتوي صفحات:
- **الرئيسية**: تعريف سريع + إحصائيات مختصرة + روابط للكتالوج ولوحة التحكم.
- **الكتالوج**: بحث/تصفية/ترتيب + إعارة وإرجاع.
- **لوحة التحكم**: إحصائيات + سجل عمليات + إدارة الكتب (إضافة/تعديل/حذف) + تصدير/استيراد JSON.
- **عن النظام**: وصف مختصر للمشروع وتطويرات مستقبلية.

## المزايا
- ✅ **إضافة/تعديل/حذف** الكتب (للأدمن فقط).
- ✅ **إعارة/إرجاع** مع تتبع عدد النسخ المتاحة.
- ✅ **بحث + تصفية حسب التصنيف + ترتيب** داخل الكتالوج.
- ✅ **إحصائيات** (عدد العناوين/إجمالي النسخ/المتاح/المُعار) + **سجل عمليات**.
- ✅ **تخزين محلي LocalStorage** + **تصدير/استيراد JSON** للبيانات.
- ✅ **وضع ليلي/نهاري**.
- ✅ **نظام تسجيل دخول بسيط** (Admin/User) للتحكم في الصلاحيات.

## الصفحات والملفات
- `index.html` — الصفحة الرئيسية  
- `catalog.html` — صفحة الكتالوج  
- `dashboard.html` — لوحة التحكم  
- `about.html` — عن النظام  
- `styles.css` — تنسيق الواجهة  
- `app.js` — منطق التطبيق (البيانات/الأحداث/التسجيل/التصدير/الاستيراد)

## التشغيل
1) ضع الملفات في نفس المجلد.
2) افتح `index.html` مباشرة بالمتصفح  
   أو شغّل سيرفر بسيط (أفضل لتفادي أي قيود بالمتصفح):

```bash
# Python
python -m http.server 8000
```

ثم افتح:
- `http://localhost:8000/index.html`

## تسجيل الدخول (Accounts)
التطبيق يجبر تسجيل الدخول عند فتح أي صفحة.

حسابات جاهزة:
- **admin** / `admin123`  (صلاحيات إدارة الكتب)
- **user** / `user123`    (عرض/إعارة/إرجاع فقط)

> تقدر تغيّر كلمات المرور/المستخدمين من داخل `app.js`.

## التصدير والاستيراد
من **لوحة التحكم**:
- **تصدير JSON**: ينزّل ملف `digital-library-export.json`
- **استيراد JSON**: يقرأ ملف JSON ويستبدل/يحدّث البيانات في التخزين المحلي.

## مفاتيح التخزين المحلي (LocalStorage)
- `dl_books_v1` — قائمة الكتب  
- `dl_log_v1` — سجل العمليات  
- `dl_theme_v1` — الثيم  
- `dl_auth_v1` — بيانات تسجيل الدخول  

## ملاحظات
- هذا المشروع **Front‑End فقط** (بدون Back‑End أو قاعدة بيانات).
- مناسب كمشروع تدريبي/جامعي، ويُمكن لاحقًا ربطه بـ API أو قاعدة بيانات.

## تطويرات مقترحة
- نظام مستخدمين وصلاحيات متقدم
- ربط قاعدة بيانات و API
- رفع ملفات PDF/EPUB ومعاينة
- تقارير وإحصاءات متقدمة

---
© مكتبة رقمية
